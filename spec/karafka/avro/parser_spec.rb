require "spec_helper"

RSpec.describe Karafka::Avro::Parser do
  it "has a version number" do
    expect(Karafka::Avro::Parser::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
